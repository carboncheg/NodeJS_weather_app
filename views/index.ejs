<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <link rel="stylesheet" href="/css/reset.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
        <link rel="stylesheet" href="/css/styles.css">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    <title>NodeJS Weather App</title>
</head>
<body>
    <div class="app">
        <div class="app__header">
            <% if (weather) { %>
            <h1><%= weather %></h1>
            <% } %>
            <% if (error) { %>
            <p class="error"><%= error %></p>
            <% } %>
        </div>
        <form action="/" method="POST" class="app__content">
            <div class="input">
                <label for="city">Город</label>
                <input type="text" id="city" placeholder="Название города" name="city">
            </div>
            <button type="submit" class="btn waves-effect waves-light right">Показать погоду</button>
        </form>
    </div>
</body>
</html>